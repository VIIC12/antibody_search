receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 10s
    send_batch_size: 1024

exporters:
  debug:
    verbosity: normal

service:
  pipelines:
    # Only traces pipeline - logs are handled by Alloy
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
