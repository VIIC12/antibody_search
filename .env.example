# Copy to .env and adjust. Compose uses these values only for host bind-mount
# interpolation. In-container paths are set in docker-compose.yml.

ABHUNTER_DB_PATH=./data
ABHUNTER_DOWNLOAD_DIR=./downloads
ABHUNTER_IGBLAST_PATH=./igblast
